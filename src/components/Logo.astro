---
import { SITE } from 'astrowind:config';
import logo from '~/assets/images/exaone-logo.svg';
---

<img
  src={logo.src}
  alt={SITE?.name || 'ExaOne Digital'}
  class="h-8 md:h-10 dark:invert"
/>
